const config = {
    page: 1,
    limit: 10,
    totalItems: 0,
    sortType: 'dsc',
    sortBy: 'updatedAt',
    searchTerm: '',
    body: '',
    cover: '',
    articleStatus: 'draft',
};

module.exports = Object.freeze(config);
